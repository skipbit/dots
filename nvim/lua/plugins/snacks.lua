
-- Only pulled in by claudecode.nvim's snacks terminal provider, which is opt-in
-- through `:ClaudeCodeProvider snacks`. Nothing here loads at startup.
return {
    'folke/snacks.nvim',
    lazy = true,
    opts = {},
}
